# Tester Smoke

The supported tester command is:

```bash
bash run_tester_smoke.sh
```

It validates the package files, creates a deterministic demo twice safely,
checks the static showcase, and verifies that the complete runtime is not
being advertised as part of the kit.
