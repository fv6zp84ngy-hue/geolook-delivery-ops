# Public Alpha 已知限制

## 产品边界

- 单机、自托管、本地文件存储；没有账号、多租户、细粒度权限或云控制面。
- 项目锁防止同一项目的进程级写冲突，但不提供多人实时协作、字段合并或冲突 UI。
- 不包含 GA4、CRM、收入归因或 OAuth；流量、询盘和商机只能作为周期信号记录。
- 不增加新的 AI 引擎，也不改变 GeoLook 的采样、站点审计和资产生成算法。
- 行业模板首期只覆盖英文官网的 SaaS、AI 软件产品和智能硬件；不覆盖电商 SKU、移动应用、游戏、医疗诊断产品或金融产品。
- 模板只提供候选判断，不进行各国法律合规判断，也不替代真实证据和人工审批。
- 业务优先级是确定性规则评分，不预测搜索量、收入、LTV 或成交概率，也不做跨客户 Benchmark。
- 容量推荐不自动配置预算、不自动批准或拒绝工单；战略优先项和资源约束仍需 Project Owner 维护。
- 业务信号只接受用户显式 CSV 导入；不做 OAuth、实时同步、跨设备识别、多触点收入归因或 GEO 因果判断。
- 原始导入 CSV 保留在本地 `imports/`，统一信号对象不会保留联系人姓名、邮箱和电话；字段不足或页面映射不唯一时只能提供项目级观察。
- 外部工具仅支持单向执行副本；不做双向实时同步、评论/附件同步、冲突解决、OAuth 组织授权或根据外部状态自动关闭内部工单。
- GitHub/Webhook 需要用户显式配置 Token/URL 并手动触发；网络失败不会改变内部交付状态。
- GEO 结果受外部 AI 平台和采样噪声影响，不承诺排名、提及、引用、流量或商机。

## 恢复边界

- `tasks.json` 使用临时文件加 `os.replace`，写入中断时保留旧文件；每次保存前最多保留 10 份 `.geo.bak/`。
- 如果用户或外部程序直接把 `tasks.json` 改成非法 JSON，系统不会猜测内容并自动覆盖；doctor 会 FAIL，需要人工选择备份恢复。
- JSONL 坏行会被跳过并报告行号，系统不会自动修复或删除坏行。
- Manifest、报告和客户账本可以重建，但 baseline 是锁定证据，缺失后不能用当前状态伪造历史快照。
- 文件系统损坏、磁盘耗尽和整机丢失仍需要用户自己的备份策略；Public Alpha 不内置远程备份。

## 部署与验收边界

- Website 部署要求公开 HTTP(S) URL；localhost、私有 IP、带凭证 URL 和项目目录外的本地证据会被拒绝。
- 超时、DNS、TLS 或网络错误保存为失败快照；系统不自动重试，也不自动修改生产站点。
- 登录墙、复杂 SPA、反爬、地域限制或第三方平台可能只能进入 Reviewer 人工判断。
- Reviewer 可以处理明确标记为 manual/data_missing 的质量门槛，但不能覆盖自动 required 的 fail/error。
- 资产文件变化、审批拒绝、部署消失或验收回归会使旧关闭证据失效并重开任务。

## Doctor 边界

- Doctor 是只读一致性检查，不修复文件，也不替代项目负责人的发布决定。
- 敏感信息扫描基于可解释规则，可能存在误报或漏报；发送客户包前仍需人工白名单检查。
- 未批准事实检查依赖 `factcheck.json` 中明确标记为 `rejected/forbidden` 的原文，不能发现事实库从未记录的新型错误。
- Doctor 扫描正式交付目录和周期账本，不扫描全部原始抓取、样本或任意外部附件。

## 验证证据边界

- Phase 7 使用真实本地文件、真实业务函数和确定性外部抓取替身，所有品牌和域名仍为虚构 fixture。
- 这证明代码路径和失败恢复可复现，不等同于真实客户可用性研究、浏览器端人工操作研究或生产流量验证。
- Public Alpha 对外发布后仍需至少一个受控客户试点，记录非技术用户的页面困惑、重复输入和操作错误。
